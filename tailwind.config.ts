import type { Config } from "tailwindcss";

const config: Config = {
  content: ["./index.html", "./src/**/*.{tsx,ts,jsx,js}"],
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;
